[package]
name = "attomath"
version = "0.1.0"
authors = ["Matthias Deiml <matthias@deiml.net>"]
edition = "2018"

[features]
default = ['serialization']

serialization = ['nom']

[dependencies]
nom = {version = "7.0", optional = true}

[dev-dependencies]
quickcheck = "1.0"
